/* Config para iPad legacy. Misma API (origen relativo) cuando se sirve desde /ipad/ */
window.CRC_LEGACY_CONFIG = {
  apiUrl: '/api/v1',
  tokenKey: 'crc_legacy_token',
  userKey: 'crc_legacy_user',
  shopKey: 'crc_legacy_shop'
};
